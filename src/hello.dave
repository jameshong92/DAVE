int main() {

	rec a = new rec(name:"james", age: 11);
	rec b = new rec(name:"james", age: 11);
	rec[] c = [a, b];

	fld q = new fld(["hell", "lo"], "name");
	fld r = new fld([1, 2, 3], "age");

	fld[] z = [q, r];
	tbl s = new tbl(z);

	r.length;
	q.length;
	s.row_length;

	int i;
	for (i=0; i<3; i++) {
		print("hi");
	}

	int asdf = 4;
	int min = min(q);
	print(min);
	return 0;

}

void print_int(int a) {
	print(a);
}