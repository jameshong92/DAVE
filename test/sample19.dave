// test table definition2
#include <dave.hpp>
int main() {
	tuple t1[] = {tuple(22, "age"), tuple("Yang", "name"), tuple(3.50, "GPA")};
    rec r1 = rec(t1); 
    tuple t2[] = {tuple(23, "age"), tuple("James", "name"), tuple(3.88, "GPA")}
    rec r2 = rec(t2);
    tuple t3[] = {tuple(24, "age"), tuple("Min Woo", "name"), tuple(3.80, "GPA")}
    rec r3 = rec(t3);
    rec r[] = {r1, r2, r3};
    tbl test = tbl(r);
	print(test);
	print(test.row_length);
	print(test.col_length);
}