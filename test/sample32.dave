// test fld type conversion
int main() {
	str path = "test.txt";
    tbl test = load(path);
    test = converse(test, 0, "string");
    print(test);
    test = converse(test, 2, "int");
    print(test);
}